require "spec_helper"

RSpec.describe User::Account::Validator do
  it "has a version number" do
    expect(User::Account::Validator::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
